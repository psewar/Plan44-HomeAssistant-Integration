# Credentials + URL for dumping devices from a plan44 bridge web API.
# Copy to devtools/.env.p44 (gitignored) and fill in your values.
# The password stays in this local file — never paste it into the chat.

P44_URL=https://your-bridge.example
P44_USER=your-web-ui-user
P44_PASSWORD=your-web-ui-password
